// Seam de validación de salidas estructuradas (JSON Schema vía
// output_config.format) antes de que un AgentResult llegue a la base de
// datos. Sin implementación concreta todavía.
export type { ParseResult, OutputParser } from '@/lib/ai/types'
